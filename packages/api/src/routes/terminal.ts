/**
 * Terminal route — removed.
 * Retained for file reference integrity; route registration is a no-op.
 */

import type { FastifyInstance } from "fastify";

export async function terminalRoutes(_server: FastifyInstance) {
  // no-op — terminal feature removed
}