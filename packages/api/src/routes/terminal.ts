/**
 * Terminal route — kaldırıldı.
 * Dosya referans bütünlüğü için korundu; route kaydı boş.
 */

import type { FastifyInstance } from "fastify";

export async function terminalRoutes(_server: FastifyInstance) {
  // no-op — terminal özelliği kaldırıldı
}